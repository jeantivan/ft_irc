#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <sstream>

bool isValidNumber(const std::string &str, int &val);
bool isValidPort(const std::string &str_port, int &port);

#endif // UTILS_HPP
